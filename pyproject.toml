[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

# Not an installable distribution: repo holds deployment scripts, Grafana
# dashboards and a standalone webhook server. Declare zero packages so
# setuptools skips flat-layout auto-discovery (it previously aborted on
# ['webhook', 'grafana']) and the toolkit's 'pip install -e .' succeeds.
[tool.setuptools]
packages = []

[tool.pylint.messages_control]
disable = [
    "E0401",  # Unable to import (Venus OS / system libs not in venv)
    "C0103",  # Invalid name (legacy naming conventions)
    "C0301",  # Line too long
    "W1203",  # f-string logging (style preference)
    "R0902",  # Too many instance attributes
    "R0903",  # Too few public methods
    "R0914",  # Too many local variables
    "R0915",  # Too many statements
    "C0116",  # Missing function docstring
    "C0413",  # Wrong import position
    "W0718",  # Catching general Exception
    "W0613",  # Unused argument
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = [
    "E722",  # Do not use bare except
    "F841",  # Local variable assigned but never used
]
